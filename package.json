{
  "name": "whispacuts",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd backend && npm ci && echo 'Backend ready'",
    "start": "cd backend && node src/server.js",
    "dev": "cd backend && npm run dev"
  }
}